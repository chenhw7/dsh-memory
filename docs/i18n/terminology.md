# Terminology

Bilingual by construction; not itself paired. Binding in both directions for every bilingual pair. Add a term only with a citable Chinese OSS/vendor precedent or a stated first-use gloss; remove a term only when its concept leaves the repository.

| English | 中文 | Notes |
|---|---|---|
| Agent Note | Agent Note | The `.agents/notes/` decision record; untranslated, like all Cordis vocabulary. |
| BM25 | BM25 | Untranslated; first use may gloss as 一种词频相关性排序算法. |
| Cairn | Cairn | The project name; never translated. |
| CJK | CJK | Untranslated. |
| client UI | 客户端 UI | |
| consistency record | 一致性记录 | The `foo.i18n.yaml` sidecar. |
| DeepSeek Harness | DeepSeek Harness | The host project; never translated. |
| decay | 衰减 | Memory recency decay. |
| extraction | 提取 | LLM auto-extraction of memories. |
| injection | 注入 | Injecting recalled memories into the prompt. |
| language switcher | 语言切换器 | The `English \| [中文](...)` line. |
| memory | 记忆 | The stored record itself. |
| namespace | 命名空间 | Settings namespace (`memory`, `memory-review`). |
| pairing contract | 配对契约 | The bilingual contract in [README.md](README.md). |
| pitfall | 踩坑记录 | A recorded failure mode; also "pitfall log" → 踩坑日志. |
| plugin | 插件 | |
| profile bundle | profile bundle | The `cordis.patch.yml` install layer; `profile` untranslated per Cordis vocabulary. |
| recall | 召回 | Retrieving memories for injection. |
| scope | 作用域 | Memory scopes (`global` / `project` / `user`); keep `global`/`project`/`user` literal. |
| session | 会话 | |
| settings | 设置 | The host settings surface. |
| sidecar | sidecar | The `.i18n.yaml` consistency file; untranslated by convention. |
| store | 存储 | The memory store; `Store` as a type name stays untranslated. |
| terminology table | 术语表 | This file. |
| tool | 工具 | dsh tool surface. |
| trim | 修剪 | Prose trimming (dsh-trim-cot-leakage context). |
| word budget | 字数预算 | Documentation length ceilings. |
| workflow | 工作流 | |
