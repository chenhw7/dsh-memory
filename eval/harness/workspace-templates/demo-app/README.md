# demo-app

A small TypeScript detail-page service. Dependencies, scripts, and lock
file are pnpm-only: `pnpm install`, `pnpm build`, `pnpm test`. Do not use
`npm install` here — a second lock file would fight the pinned one.
